require('dotenv').config(); const bcrypt = require('bcryptjs'); const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_KEY); async function test() { const { data } = await supabase.from('users').select('*').eq('email', 'owner@workforceos.com').single(); const isMatch = await bcrypt.compare('password1234', data.password); console.log('Match:', isMatch); } test();  
